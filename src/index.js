import React from 'react';
import ReactDOM from 'react-dom/client';
import Contatos from './home/contatos';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <Contatos/>
  </React.StrictMode>
);

